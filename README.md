The idea is that: on a central server the vehicles will be communicating and post their progress.
In hardware, 
- `uav_control` will be in drone's jetson computer.
- `ugv control` will be in ugv's primary computer.
- `central` will be in central manager laptop. 

seperate README files are inside each folder describing how to use them. 